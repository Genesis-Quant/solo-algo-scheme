[project]
name = "solo-runtime-fixture"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["scheme>=1,<2", "pandas>=2.2,<3"]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"
